# F1 Guess 联机模式改造需求文档

> 基于 v1.0 单机版本，新增联机对战模式
> 生成时间: 2026-07-28

---

## 目录

- [1. 需求概述](#1-需求概述)
- [2. 核心玩法](#2-核心玩法)
- [3. 功能设计](#3-功能设计)
- [4. 技术架构](#4-技术架构)
- [5. UI 设计](#5-ui-设计)
- [6. 通信协议](#6-通信协议)
- [7. 实施计划](#7-实施计划)
- [8. 技术挑战](#8-技术挑战)
- [9. 验收标准](#9-验收标准)

---

## 1. 需求概述

### 1.1 项目背景

当前 F1 Guess 为纯前端单机游戏（v1.0），玩家通过 8 次机会猜测隐藏车手。本次改造新增**联机对战模式**，让两位玩家可以实时对战。

### 1.2 改造目标

- 新增**联机对战模式**（同步猜测竞赛）
- 保留现有**单机模式**（更名为"单机模式"）
- 首页提供模式选择入口

### 1.3 版本信息

- **当前版本**: v1.0 (单机版)
- **目标版本**: v2.0 (单机 + 联机)

---

## 2. 核心玩法

### 2.1 单机模式（保持不变）

- 单人游戏，随机目标车手
- 8 次猜测机会
- 数据统计（localStorage）

### 2.2 联机对战模式

**玩法定义**：两名玩家**同步猜测同一位车手**，比谁**用更少次数**猜中。

**核心规则**：

| 维度 | 规则 |
|:---|:---|
| **玩家数** | 2 人（1v1） |
| **猜测次数** | 每人 8 次机会 |
| **目标车手** | 系统随机分配，两人相同 |
| **时间限制** | **单局总时长 2 分钟（120 秒）** |
| **胜利条件** | 优先猜中者胜；时间到时未分胜负则按猜测次数少者胜；次数相同为平局 |
| **失败条件** | 8 次用完未猜中 或 时间到 |
| **实时同步** | 双方实时看到对方猜测次数和颜色反馈（**不看具体姓名**） |

**胜负判定优先级**：

```
1. 先猜中者胜
2. 双方都未猜中且时间到 → 猜测次数少者胜
3. 猜测次数相同 → 平局
4. 双方都 8 次用完 → 平局
```

**时间到处理**：

- 倒计时归零时，立即结束游戏
- 已猜中者胜（如果有）
- 都未猜中则按猜测次数判定
- 显示倒计时读秒（最后 10 秒变红）

### 2.3 联机模式流程

```
玩家 A                系统                  玩家 B
  │                    │                     │
  ├─ 创建房间 ────────►│                     │
  │◄──── 房间号 ───────┤                     │
  │  (6 位字母)         │                     │
  │                    │◄── 加入房间 ────────┤
  │                    │                     │
  │                    ├─ 双方就绪 ─────────►│
  │◄──── 游戏开始 ─────┤                     │
  │                    ├─ 分配目标车手 ─────►│
  │                    │  (2 分钟倒计时开始)  │
  │                    │                     │
  ├─ 猜测 ────────────►│                     │
  │                    ├─ 转发对手进度 ─────►│
  │                    │                     │
  │                    │◄──── 猜测 ──────────┤
  │◄──── 转发对手进度 ─┤                     │
  │                    │                     │
  │  ... (轮流进行)                          │
  │                    │                     │
  ├─ 猜中 ────────────►│                     │
  │                    ├─ 通知游戏结束 ─────►│
  │◄──── 胜负结果 ─────┤                     │
  │                    │                     │
  或:                  │                     │
  │                    │                     │
  │              (2 分钟时间到)               │
  │                    │                     │
  │◄──── 强制结束 ─────┤                     │
  │                    ├─ 判定胜负 ─────────►│
```

---

## 3. 功能设计

### 3.1 房间系统

| 功能 | 说明 |
|:---|:---|
| **创建房间** | 玩家输入昵称，系统生成 6 位字母房间号 |
| **加入房间** | 输入房间号 + 昵称加入 |
| **房间号规则** | 6 位大写字母（排除易混淆字符 I/O/0/1） |
| **房间生命周期** | 创建后 5 分钟无人加入自动销毁；游戏中断线 30 秒未重连自动结束 |
| **断线重连** | 刷新页面后可通过房间号 + 昵称重连 |

### 3.2 游戏同步

**同步内容**：

- 对方**猜测次数**（实时更新）
- 对方**最新猜测的颜色反馈**（不看具体姓名，保护公平性）
- 对方**游戏状态**（进行中/已完成）
- **倒计时**（服务器统一计时，定期广播）

**不同步内容**：

- 目标车手 ID（直到游戏结束）
- 对方具体猜测了哪位车手

### 3.3 倒计时系统

| 项目 | 规则 |
|:---|:---|
| **总时长** | 120 秒（2 分钟） |
| **开始时机** | 双方就绪，目标车手分配后立即开始 |
| **显示位置** | 游戏界面顶部居中，大号字体 |
| **视觉反馈** | 正常: 白色; ≤30s: 黄色; ≤10s: 红色闪烁 |
| **结束处理** | 时间到立即锁定输入，触发结算逻辑 |
| **同步机制** | 服务器每秒广播剩余时间（防客户端篡改） |

### 3.4 结算规则

| 场景 | 结果 |
|:---|:---|
| 一方猜中 | 猜中者胜 |
| 双方同时猜中（同一秒） | 平局 |
| 时间到，双方均未猜中 | 猜测次数少者胜 |
| 时间到，猜测次数相同 | 平局 |
| 一方掉线 30 秒未重连 | 另一方胜 |

### 3.5 结算展示

- 显示胜负结果（你赢了/你输了/平局）
- 显示双方猜测次数和用时
- 显示目标车手完整信息
- 提供"再来一局"按钮（双方确认后重新开始）

---

## 4. 技术架构

### 4.1 整体架构

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│  玩家 A      │ ◄─────► │              │ ◄─────► │  玩家 B      │
│  (React)    │  WS     │  后端服务器   │  WS     │  (React)    │
└─────────────┘         │  (Workers)   │         └─────────────┘
                        │              │
                        │  - 房间管理   │
                        │  - 状态同步   │
                        │  - 游戏逻辑   │
                        │  - 倒计时管理 │
                        └──────────────┘
                              │
                              ▼
                        ┌──────────────┐
                        │ Durable      │
                        │ Objects      │
                        │ (房间状态)   │
                        └──────────────┘
```

### 4.2 技术选型

**前端（在现有基础上扩展）**：

| 类别 | 技术 | 用途 |
|:---|:---|:---|
| 路由 | React Router v6 | 页面路由（首页/单机/联机） |
| 实时通信 | 原生 WebSocket API | 与后端双向通信 |
| 状态管理 | useState + useReducer | 联机游戏状态 |
| 倒计时 | requestAnimationFrame | 客户端平滑倒计时显示 |

**后端（新增）**：

| 类别 | 技术 | 用途 |
|:---|:---|:---|
| 计算平台 | Cloudflare Workers | 边缘部署，全球低延迟 |
| 状态持久化 | Durable Objects | 房间状态强一致性存储 |
| 实时通信 | WebSocket (Workers 原生) | 双向实时通信 |
| 语言 | TypeScript | 类型安全 |

### 4.3 项目结构

```
F1 Guess/
├── f1-guess/                  # 前端 (现有)
│   ├── src/
│   │   ├── components/
│   │   │   ├── solo/          # 单机模式组件 (从现有迁移)
│   │   │   ├── online/        # 联机模式组件 (新增)
│   │   │   │   ├── LobbyView.tsx
│   │   │   │   ├── RoomWaitView.tsx
│   │   │   │   ├── OnlineGameView.tsx
│   │   │   │   ├── OpponentProgress.tsx
│   │   │   │   ├── Countdown.tsx
│   │   │   │   └── ResultModal.tsx
│   │   ├── hooks/
│   │   │   ├── useGame.ts     # 单机 (现有)
│   │   │   ├── useOnlineGame.ts  # 联机 (新增)
│   │   │   └── useWebSocket.ts   # WS 连接 (新增)
│   │   ├── pages/
│   │   │   ├── HomePage.tsx   # 模式选择 (新增)
│   │   │   ├── SoloPage.tsx   # 单机 (现有 App 迁移)
│   │   │   └── OnlinePage.tsx # 联机 (新增)
│   │   ├── router.tsx         # 路由配置 (新增)
│   │   └── ...
│   └── ...
├── server/                    # 后端 (新增)
│   ├── src/
│   │   ├── index.ts           # Workers 入口
│   │   ├── room.ts            # 房间管理 Durable Object
│   │   ├── game.ts            # 游戏逻辑
│   │   ├── timer.ts           # 倒计时管理
│   │   ├── types.ts           # 类型定义
│   │   └── utils.ts           # 工具函数
│   ├── package.json
│   ├── wrangler.toml          # Cloudflare 配置
│   ├── tsconfig.json
│   └── README.md
└── package.json               # monorepo 根 (新增, workspaces)
```

### 4.4 部署架构

```
┌─────────────────────────────────────┐
│   f1-guess-game.pages.dev           │
│   (Cloudflare Pages - 前端)          │
└─────────────────────────────────────┘
              │
              │ wss://api.f1-guess-game.workers.dev/room/:id
              ▼
┌─────────────────────────────────────┐
│   f1-guess-game-api.workers.dev     │
│   (Cloudflare Workers - 后端)        │
│   - Durable Objects (房间状态)       │
│   - WebSocket (实时通信)             │
│   - 倒计时服务                        │
└─────────────────────────────────────┘
```

---

## 5. UI 设计

### 5.1 首页（模式选择）

```
┌───────────────────────────────────┐
│       F1 GUESS                    │
│   F1车手猜谜游戏                  │
│                                   │
│  ┌─────────────┐ ┌─────────────┐ │
│  │  单机模式   │ │  联机模式   │ │
│  │  单人挑战   │ │  1v1 对战   │ │
│  └─────────────┘ └─────────────┘ │
│                                   │
│  数据截止到 2026-07-27, 共 109 名车手│
└───────────────────────────────────┘
```

### 5.2 联机大厅

```
┌───────────────────────────────────┐
│  联机模式                   [返回]│
│                                   │
│  你的名字: [____________]         │
│                                   │
│  ┌─────────────────────────────┐ │
│  │     创建新房间              │ │
│  └─────────────────────────────┘ │
│                                   │
│  ─────────── 或 ───────────       │
│                                   │
│  房间号: [______]                 │
│  ┌─────────────────────────────┐ │
│  │     加入房间                │ │
│  └─────────────────────────────┘ │
└───────────────────────────────────┘
```

### 5.3 房间等待

```
┌───────────────────────────────────┐
│  房间号: ABCXYZ          [复制]   │
│                                   │
│  玩家列表:                        │
│  ✅ 你 (玩家A)                    │
│  ⏳ 等待对手加入...               │
│                                   │
│  将房间号分享给朋友开始游戏       │
└───────────────────────────────────┘
```

### 5.4 联机游戏界面

```
┌───────────────────────────────────┐
│           ⏱️ 01:23               │  ← 倒计时 (≤10s 变红)
│                                   │
│  你: 3 次       vs       对手: 5 次│
│  ────────────────────────────────│
│  输入车手名字(中文或英文)...      │
│  ────────────────────────────────│
│  你的猜测记录:                    │
│  ┌─────────────────────────────┐ │
│  │ 🟩🟨⬜⬜⬜⬜⬜⬜ 姓名      │ │
│  │ ⬜🟩⬜⬜⬜⬜⬜⬜ 姓名      │ │
│  └─────────────────────────────┘ │
│  ────────────────────────────────│
│  对手最新猜测:                    │
│  ⬜🟨🟩⬜⬜⬜⬜⬜                 │  ← 只看颜色不看姓名
│                                   │
│  [放弃]                           │
└───────────────────────────────────┘
```

### 5.5 结算弹窗

```
┌───────────────────────────────────┐
│         🎉 你赢了！               │
│                                   │
│  你:    4 次  (用时 1'23")        │
│  对手:  6 次  (未完成)            │
│                                   │
│  目标车手:                        │
│  刘易斯·汉密尔顿 (Lewis Hamilton) │
│  法拉利, 44 号, 7 冠...           │
│                                   │
│  [再来一局]  [返回大厅]           │
└───────────────────────────────────┘
```

---

## 6. 通信协议

### 6.1 WebSocket 连接

**连接地址**: `wss://api.f1-guess-game.workers.dev/room/:roomId?playerId=:playerId&playerName=:name`

### 6.2 消息类型

#### 客户端 → 服务器

```typescript
// 创建房间
{ type: 'create_room', playerName: string }

// 加入房间
{ type: 'join_room', roomId: string, playerName: string }

// 提交猜测
{ type: 'make_guess', driverId: string }

// 放弃游戏
{ type: 'give_up' }

// 请求重新开始
{ type: 'request_restart' }

// 确认重新开始
{ type: 'confirm_restart' }

// 心跳
{ type: 'ping' }
```

#### 服务器 → 客户端

```typescript
// 房间创建成功
{ type: 'room_created', roomId: string, playerId: string }

// 加入房间成功
{ type: 'room_joined', roomId: string, playerId: string, opponent: { name: string } }

// 对手加入
{ type: 'opponent_joined', opponent: { name: string } }

// 游戏开始
{ type: 'game_start', duration: 120 }  // 不发送 targetDriverId!

// 对手猜测更新
{ type: 'opponent_guess', guessCount: number, feedback: GuessFeedback }

// 对手完成游戏
{ type: 'opponent_finished', status: 'won'|'lost', guessCount: number }

// 倒计时同步 (每秒)
{ type: 'timer_sync', remaining: number }

// 游戏结束
{
  type: 'game_end',
  result: 'win'|'lose'|'tie',
  reason: 'guessed'|'timeout'|'opponent_disconnect',
  yourGuesses: number,
  opponentGuesses: number,
  targetDriver: Driver,  // 游戏结束才发送
  duration: number
}

// 对手请求重新开始
{ type: 'opponent_request_restart' }

// 重新开始
{ type: 'game_restart' }

// 对手离开
{ type: 'opponent_left' }

// 错误
{ type: 'error', code: string, message: string }
```

### 6.3 错误码

| 错误码 | 说明 |
|:---|:---|
| `ROOM_NOT_FOUND` | 房间号不存在 |
| `ROOM_FULL` | 房间已满（已有 2 人） |
| `GAME_IN_PROGRESS` | 游戏进行中无法加入 |
| `INVALID_GUESS` | 无效的猜测（重复/超限） |
| `NOT_YOUR_TURN` | 非你的回合（预留，同步模式暂不需要） |

---

## 7. 实施计划

### 7.1 阶段划分

| 阶段 | 目标 | 工期 | 关键产出 |
|:---|:---|:---|:---|
| **阶段 1** | 前端架构改造 | 1-2 天 | React Router 集成、页面拆分、单机模式迁移 |
| **阶段 2** | 后端开发 | 2-3 天 | Workers 项目、房间管理、游戏逻辑、倒计时服务 |
| **阶段 3** | 前端联机 | 2-3 天 | WebSocket Hook、联机 UI、倒计时组件 |
| **阶段 4** | 联调优化 | 1-2 天 | 断线重连、错误处理、性能优化 |
| **阶段 5** | 部署上线 | 1 天 | 生产部署、测试验证、文档更新 |

**总工期**: 7-11 天

### 7.2 详细任务

#### 阶段 1：前端架构改造（1-2 天）

- [ ] 安装 React Router
- [ ] 创建路由配置 (`router.tsx`)
- [ ] 创建首页 (`HomePage.tsx`) - 模式选择
- [ ] 迁移现有 App 到 `SoloPage.tsx`
- [ ] 创建 `OnlinePage.tsx` 占位
- [ ] 更新构建配置
- [ ] 提交并验证部署

#### 阶段 2：后端开发（2-3 天）

- [ ] 初始化 Workers 项目 (`server/`)
- [ ] 配置 wrangler.toml
- [ ] 实现房间管理 Durable Object
  - [ ] 创建/加入/查询房间
  - [ ] 房间状态持久化
  - [ ] 房间超时清理
- [ ] 实现 WebSocket 连接处理
  - [ ] 连接建立/断开
  - [ ] 消息路由
  - [ ] 心跳检测
- [ ] 实现游戏逻辑
  - [ ] 目标车手分配
  - [ ] 猜测转发
  - [ ] 胜负判定
- [ ] 实现倒计时服务
  - [ ] 每秒广播
  - [ ] 时间到触发结算
- [ ] 本地测试

#### 阶段 3：前端联机（2-3 天）

- [ ] 实现 `useWebSocket` Hook
  - [ ] 连接管理
  - [ ] 消息收发
  - [ ] 自动重连
- [ ] 实现 `useOnlineGame` Hook
  - [ ] 房间状态管理
  - [ ] 游戏状态同步
  - [ ] 倒计时本地显示
- [ ] 联机大厅 UI (`LobbyView.tsx`)
- [ ] 房间等待 UI (`RoomWaitView.tsx`)
- [ ] 联机游戏 UI (`OnlineGameView.tsx`)
  - [ ] 自己的猜测表格（复用单机）
  - [ ] 对手进度显示
  - [ ] 倒计时组件 (`Countdown.tsx`)
- [ ] 结算弹窗 (`ResultModal.tsx`)

#### 阶段 4：联调优化（1-2 天）

- [ ] 端到端测试（开两个浏览器标签）
- [ ] 断线重连测试
- [ ] 房间超时清理测试
- [ ] 错误处理完善
- [ ] 性能优化（消息节流）
- [ ] 防作弊验证（目标车手不泄露）

#### 阶段 5：部署上线（1 天）

- [ ] Workers 生产部署
- [ ] 前端环境变量配置（API 地址）
- [ ] CORS 配置
- [ ] 端到端验证
- [ ] 更新 README
- [ ] 发布公告

---

## 8. 技术挑战

### 8.1 Durable Objects 一致性

**问题**：房间状态需要强一致性，避免并发冲突。

**方案**：
- 使用 Durable Objects 的事务 API
- 每个房间映射到一个 DO 实例（单线程串行处理）
- 状态变更先在 DO 内完成再广播

### 8.2 WebSocket 断线重连

**问题**：网络波动导致连接断开，需要无感重连。

**方案**：
- 客户端实现心跳（每 30 秒 ping）
- 断开自动重连（指数退避）
- 重连后通过 `playerId` 恢复状态
- 服务器端保留 30 秒重连窗口

### 8.3 倒计时同步

**问题**：客户端本地倒计时不准，可能被篡改。

**方案**：
- 服务器权威计时，每秒广播剩余时间
- 客户端用 `requestAnimationFrame` 平滑显示
- 服务器时间为准，客户端只做展示
- 时间到由服务器触发结算

### 8.4 防作弊

**问题**：目标车手 ID 不能泄露给客户端。

**方案**：
- 游戏开始时**不发送** `targetDriverId`
- 服务器端验证猜测是否正确
- 只返回颜色反馈，不返回目标信息
- 游戏结束后才发送完整 `targetDriver` 对象

### 8.5 房间清理

**问题**：超时未活动的房间占用资源。

**方案**：
- 创建后 5 分钟无人加入自动销毁
- 游戏中 30 秒无心跳自动结束
- Durable Objects 自带 TTL 机制

### 8.6 CORS 跨域

**问题**：前端 Pages 和后端 Workers 域名不同。

**方案**：
- Workers 响应添加 CORS 头
- 允许 `f1-guess-game.pages.dev` 域名
- WebSocket 不受同源策略限制

---

## 9. 验收标准

### 9.1 功能验收

- [ ] 首页可以选择单机/联机模式
- [ ] 联机模式可以创建/加入房间
- [ ] 房间号可以正确复制分享
- [ ] 双方同时开始游戏，目标车手相同
- [ ] 实时看到对方猜测次数和颜色反馈
- [ ] 倒计时正常显示，≤10s 变红
- [ ] 时间到自动结算，胜负判定正确
- [ ] 结算弹窗显示双方数据和目标车手
- [ ] 可以"再来一局"（双方确认）
- [ ] 断线重连正常（30 秒内）
- [ ] 单机模式功能不受影响

### 9.2 性能验收

- [ ] WebSocket 延迟 < 200ms
- [ ] 倒计时误差 < 1 秒
- [ ] 房间创建/加入响应 < 1 秒
- [ ] 同时在线 100+ 房间无压力

### 9.3 兼容性验收

- [ ] 桌面端 Chrome/Edge/Firefox/Safari 正常
- [ ] 移动端 Safari/Chrome 正常
- [ ] 不支持 WebSocket 的浏览器给出提示

### 9.4 安全验收

- [ ] 目标车手 ID 在游戏结束前不出现在网络包中
- [ ] 无法通过修改客户端代码作弊
- [ ] 房间号不可被遍历猜测

---

## 10. 后续扩展（可选）

完成 v2.0 后，可以考虑：

- **多人房间**（3-10 人竞速）
- **排行榜**（全服/好友排行）
- **每日挑战**（全服同一车手）
- **观战模式**（看别人对战）
- **战绩系统**（联机胜率、平均猜测数）
- **语音/表情互动**

---

## 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|:---|:---|:---|:---|
| v2.0-draft | 2026-07-28 | 初始版本，联机模式改造需求 | Claude |

---

**文档结束**
